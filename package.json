{
  "name": "fortune-weaver",
  "version": "0.1.0",
  "private": true,
  "description": "Fortune Weaver — a route & team planner for Fire Emblem: Fortune's Weave",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c green,cyan \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client && node scripts/copy-images.mjs",
    "start": "npm run start -w server",
    "preview": "npm run preview -w client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
